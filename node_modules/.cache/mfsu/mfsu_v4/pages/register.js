"use strict";
import { jsx, jsxs } from "react/jsx-runtime";
import { useIntl, FormattedMessage, setLocale, getLocale } from "umi";
import { useState } from "react";
import styles from "./register.module.scss";
export default function RegisterPage() {
  const intl = useIntl();
  const currentLocale = getLocale();
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [confirm, setConfirm] = useState("");
  const [status, setStatus] = useState("idle");
  const handleSubmit = (e) => {
    e.preventDefault();
    if (!email.trim() || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
      setStatus("error-email");
      return;
    }
    if (password.length < 8) {
      setStatus("error-password");
      return;
    }
    if (password !== confirm) {
      setStatus("error-mismatch");
      return;
    }
    setStatus("loading");
    setTimeout(() => {
      if (email === "test@example.com") {
        setStatus("error-duplicate");
        return;
      }
      setStatus("success");
    }, 1500);
  };
  if (status === "success") {
    return /* @__PURE__ */ jsx("div", { className: styles.page, children: /* @__PURE__ */ jsxs("div", { className: styles.successCard, children: [
      /* @__PURE__ */ jsx("div", { className: styles.checkIcon, children: "\u2713" }),
      /* @__PURE__ */ jsx("h1", { children: /* @__PURE__ */ jsx(FormattedMessage, { id: "register.success.title" }) }),
      /* @__PURE__ */ jsx("p", { children: /* @__PURE__ */ jsx(FormattedMessage, { id: "register.success.desc" }) }),
      /* @__PURE__ */ jsx("a", { href: "/", children: /* @__PURE__ */ jsx(FormattedMessage, { id: "register.success.back" }) })
    ] }) });
  }
  const isLoading = status === "loading";
  const errorId = status.startsWith("error-") ? `register.${status}` : null;
  return /* @__PURE__ */ jsxs("div", { className: styles.page, children: [
    /* @__PURE__ */ jsxs("div", { className: styles.top, children: [
      /* @__PURE__ */ jsx("a", { href: "/", className: styles.brand, children: /* @__PURE__ */ jsx(FormattedMessage, { id: "nav.brand" }) }),
      /* @__PURE__ */ jsx("button", { className: styles.langBtn, onClick: () => setLocale(currentLocale === "zh-CN" ? "en-US" : "zh-CN", false), children: currentLocale === "zh-CN" ? "EN" : "\u4E2D\u6587" })
    ] }),
    /* @__PURE__ */ jsxs("div", { className: styles.formCard, children: [
      /* @__PURE__ */ jsx("h1", { children: /* @__PURE__ */ jsx(FormattedMessage, { id: "register.title" }) }),
      /* @__PURE__ */ jsxs("form", { onSubmit: handleSubmit, children: [
        /* @__PURE__ */ jsx("label", { children: /* @__PURE__ */ jsx(FormattedMessage, { id: "register.email.label" }) }),
        /* @__PURE__ */ jsx("input", { type: "email", placeholder: intl.formatMessage({ id: "register.email.placeholder" }), value: email, onChange: (e) => {
          setEmail(e.target.value);
          setStatus("idle");
        } }),
        /* @__PURE__ */ jsx("label", { children: /* @__PURE__ */ jsx(FormattedMessage, { id: "register.password.label" }) }),
        /* @__PURE__ */ jsx("input", { type: "password", placeholder: intl.formatMessage({ id: "register.password.placeholder" }), value: password, onChange: (e) => {
          setPassword(e.target.value);
          setStatus("idle");
        } }),
        /* @__PURE__ */ jsx("label", { children: /* @__PURE__ */ jsx(FormattedMessage, { id: "register.confirm.label" }) }),
        /* @__PURE__ */ jsx("input", { type: "password", placeholder: intl.formatMessage({ id: "register.confirm.placeholder" }), value: confirm, onChange: (e) => {
          setConfirm(e.target.value);
          setStatus("idle");
        } }),
        errorId && /* @__PURE__ */ jsx("p", { className: styles.error, children: intl.formatMessage({ id: errorId }) }),
        /* @__PURE__ */ jsx("button", { type: "submit", disabled: isLoading, className: isLoading ? styles.btnDisabled : styles.btn, children: isLoading ? intl.formatMessage({ id: "register.submitting" }) : intl.formatMessage({ id: "register.submit" }) })
      ] }),
      /* @__PURE__ */ jsxs("p", { className: styles.loginLink, children: [
        /* @__PURE__ */ jsx(FormattedMessage, { id: "register.login.text" }),
        " ",
        /* @__PURE__ */ jsx("a", { href: "#", children: /* @__PURE__ */ jsx(FormattedMessage, { id: "register.login.link" }) })
      ] })
    ] })
  ] });
}
